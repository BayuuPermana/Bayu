# [t5_global] Global Cleanup Implementation Plan

## Overview
This plan focuses on the final global cleanup of the codebase, removing unused CSS animations and performing a final audit for "AI slop" and technical debt. This is the final step in the "Eliminate AI Slop Design" epic.

## Scope Definition (CRITICAL)
### In Scope
- Remove unused marquee keyframes and theme definitions from `index.css`.
- Perform a final audit of all refactored components for consistency.
- Final build and deployment check.
### Out of Scope (DO NOT TOUCH)
- Adding new features.
- Modifying component logic.

## Current State Analysis
- `index.css`: Contains unused `marquee` and `marquee-reverse` animations (L7-21).
- `TechStack.tsx`: Now uses a grid, rendering marquee CSS obsolete.

## Implementation Phases

### Phase 1: CSS Cleanup
- **Goal**: Remove dead code from the global stylesheet.
- **Steps**:
  1. [x] Remove `@theme` block from `index.css`.
- **Verification**: Ensure site styles remain intact (except for removed animations).

### Phase 2: Final Audit & Consistency
- **Goal**: Ensure all components follow the new professional tone and style.
- **Steps**:
  1. [x] Audit `Hero.tsx`, `About.tsx`, `Projects.tsx`, `TechStack.tsx`, `Contact.tsx`.
  2. [x] Check for any remaining redundant Tailwind classes.
- **Verification**: `npm run build`.

### Phase 3: Final Final Build
- **Goal**: Verify production readiness.
- **Steps**:
  1. [x] Run `npm run build`.
- **Verification**: Check build output for errors.

## Review Criteria (Self-Critique)
- Simple and focused? Yes.
- Targets specific dead code? Yes.
